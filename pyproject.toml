[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "learned-branching-micrograd"
dependencies = [
    "cvxpy",
    "numpy",
    "matplotlib",
    "scipy",
    "torch",
    "torchvision",
]
requires-python = ">= 3.12"
